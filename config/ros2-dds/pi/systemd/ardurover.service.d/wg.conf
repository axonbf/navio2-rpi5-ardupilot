# Order ardurover after WireGuard so its telemetry UDP socket to the GCS
# (udp:10.0.0.2:14551) is created only once wg0 exists. Ordering only:
# After= (not Requires=) so ardurover ALWAYS starts even if wg0 is absent/fails.
[Unit]
After=wg-quick@wg0.service
